import { Provider } from '@ai16z/eliza';

export interface HeliusProvider extends Provider {
  // Helius specific provider properties
}

// Helius API Provider
export const heliusProvider = {
  // Implementation for Helius API integration
};
