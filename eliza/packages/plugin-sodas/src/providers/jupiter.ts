import { Provider } from '@ai16z/eliza';

export interface JupiterProvider extends Provider {
  // Jupiter specific provider properties
}

// Jupiter API Provider
export const jupiterProvider = {
  // Implementation for Jupiter API integration
};
